# Character Images

Static character portrait images served at `/characters/<name>.png` (or .webp).

Run `scripts/download-characters.sh` from the project root to populate this folder,
or place images here manually before deploying.
